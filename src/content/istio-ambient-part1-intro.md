---
title: "Istio Ambient Part 1: Sidecar 없는 Service Mesh"
excerpt: "Istio 1.24 GA된 Ambient Mode - ztunnel과 waypoint로 80-90% 리소스 절감"
category: "kubernetes"
tags: ["istio", "ambient-mesh", "ztunnel", "waypoint", "service-mesh", "kubernetes"]
series:
  name: "istio-ambient"
  order: 1
date: "2024-12-24"
---

## 🎯 시작하며

지금까지 배운 Istio는 모두 **Sidecar 방식**이었습니다. 각 Pod마다 Envoy Sidecar가 함께 배포되어 트래픽을 처리했죠. 하지만 2024년 11월, Istio 1.24에서 **Ambient Mode**가 정식(GA) 출시되었습니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                  Sidecar vs Ambient 핵심 차이                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Sidecar 방식                    Ambient 방식                  │
│   ════════════                    ════════════                  │
│                                                                 │
│   ┌─────────────┐                 ┌─────────────┐               │
│   │    Pod      │                 │    Pod      │               │
│   │  ┌───────┐  │                 │  ┌───────┐  │               │
│   │  │ App   │  │                 │  │ App   │  │  ← Sidecar ❌ │
│   │  └───┬───┘  │                 │  └───────┘  │               │
│   │      │      │                 └─────────────┘               │
│   │  ┌───▼───┐  │                        │                      │
│   │  │Envoy  │  │ ← 각 Pod마다           │                      │
│   │  │Sidecar│  │   Sidecar 필요         ▼                      │
│   │  └───────┘  │                 ┌─────────────┐               │
│   └─────────────┘                 │  ztunnel    │ ← Node당 1개  │
│                                   │ (DaemonSet) │               │
│   Pod 10개 = Sidecar 10개         └─────────────┘               │
│                                                                 │
│   리소스: 높음                    리소스: 80-90% 절감           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

학습하면서 궁금했던 것들입니다:
- Sidecar 없이 어떻게 mTLS가 가능할까?
- ztunnel과 waypoint는 무엇일까?
- 왜 Ambient를 도입해야 할까?

---

## 💡 Sidecar 방식의 한계

### 리소스 오버헤드

```
┌─────────────────────────────────────────────────────────────────┐
│                   Sidecar 리소스 사용량                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Pod당 Sidecar 기본 리소스:                                    │
│   - CPU: 10m ~ 2000m (요청에 따라)                              │
│   - Memory: 128Mi ~ 1Gi                                         │
│                                                                 │
│   예시: 100개 Pod 클러스터                                      │
│   ┌───────────────────────────────────────────────────────┐     │
│   │  애플리케이션 리소스                                  │     │
│   │  CPU: 5000m, Memory: 10Gi                            │     │
│   │                                                       │     │
│   │  Sidecar 리소스 (100개)                              │     │
│   │  CPU: 1000m ~ 5000m, Memory: 12.8Gi ~ 50Gi           │     │
│   │                                                       │     │
│   │  ⚠️ Sidecar가 앱보다 리소스를 더 쓸 수 있음!         │     │
│   └───────────────────────────────────────────────────────┘     │
│                                                                 │
│   Ambient 모드:                                                 │
│   ┌───────────────────────────────────────────────────────┐     │
│   │  ztunnel (Node당 1개, 예: 5 Node)                     │     │
│   │  CPU: 100m × 5 = 500m                                 │     │
│   │  Memory: 256Mi × 5 = 1.28Gi                           │     │
│   │                                                       │     │
│   │  💰 80-90% 리소스 절감!                               │     │
│   └───────────────────────────────────────────────────────┘     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 운영 복잡성

```
┌─────────────────────────────────────────────────────────────────┐
│                   Sidecar 운영 어려움                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   1. Sidecar Injection 관리                                     │
│      - 어떤 Pod에 inject할지 결정                               │
│      - 레이블 관리: istio-injection=enabled                     │
│      - 문제 발생 시 롤백 어려움                                 │
│                                                                 │
│   2. Sidecar 업그레이드                                         │
│      - Istio 버전 업그레이드 = Pod 재시작 필요                  │
│      - 1000개 Pod = 1000개 재시작                               │
│      - 다운타임 또는 롤링 업데이트 필요                         │
│                                                                 │
│   3. 리소스 튜닝                                                │
│      - Pod마다 Sidecar 리소스 설정 필요                         │
│      - 트래픽 패턴에 따라 다름                                  │
│      - 과소 설정 → 성능 저하                                    │
│      - 과대 설정 → 낭비                                         │
│                                                                 │
│   4. 디버깅 복잡성                                              │
│      - 요청 경로: App → Sidecar → Network → Sidecar → App       │
│      - 문제 위치 파악 어려움                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🆕 Ambient Mode 아키텍처

### 핵심 컴포넌트

```
┌─────────────────────────────────────────────────────────────────┐
│                   Ambient Mode 아키텍처                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                    Control Plane                        │   │
│   │                       istiod                            │   │
│   └─────────────────────────────────────────────────────────┘   │
│                             │                                   │
│              ┌──────────────┴──────────────┐                    │
│              ▼                             ▼                    │
│   ┌─────────────────────┐       ┌─────────────────────┐         │
│   │      ztunnel        │       │     waypoint        │         │
│   │   (DaemonSet)       │       │    (Deployment)     │         │
│   │                     │       │                     │         │
│   │  • Node당 1개       │       │  • Namespace당      │         │
│   │  • L4 처리          │       │  • L7 처리          │         │
│   │  • mTLS             │       │  • 라우팅, 정책     │         │
│   │  • 기본 AuthZ       │       │  • JWT, 헤더 조작   │         │
│   └─────────────────────┘       └─────────────────────┘         │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                     Application Pods                    │   │
│   │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐     │   │
│   │  │  App A  │  │  App B  │  │  App C  │  │  App D  │     │   │
│   │  │(Sidecar │  │(Sidecar │  │(Sidecar │  │(Sidecar │     │   │
│   │  │  없음)  │  │  없음)  │  │  없음)  │  │  없음)  │     │   │
│   │  └─────────┘  └─────────┘  └─────────┘  └─────────┘     │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### ztunnel (Zero Trust Tunnel)

```
┌─────────────────────────────────────────────────────────────────┐
│                        ztunnel                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   역할:                                                         │
│   ═════                                                         │
│   • L4 (TCP) 트래픽 처리                                        │
│   • mTLS (상호 인증, 암호화)                                    │
│   • 기본 AuthorizationPolicy (IP, 포트 기반)                    │
│   • Telemetry (메트릭, 로그)                                    │
│                                                                 │
│   배포 방식:                                                    │
│   ═════════                                                     │
│   • DaemonSet (모든 Node에 1개씩)                               │
│   • Rust로 작성 (고성능, 저메모리)                              │
│   • Node의 모든 Pod 트래픽 처리                                 │
│                                                                 │
│   트래픽 인터셉트:                                              │
│   ═══════════════                                               │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                       Node                              │   │
│   │                                                         │   │
│   │  ┌─────────┐    ┌─────────┐    ┌─────────┐              │   │
│   │  │  Pod A  │    │  Pod B  │    │  Pod C  │              │   │
│   │  └────┬────┘    └────┬────┘    └────┬────┘              │   │
│   │       │              │              │                   │   │
│   │       └──────────────┼──────────────┘                   │   │
│   │                      ▼                                  │   │
│   │              ┌───────────────┐                          │   │
│   │              │   ztunnel     │ ← eBPF로 트래픽 리다이렉트│   │
│   │              └───────────────┘                          │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### waypoint Proxy

```
┌─────────────────────────────────────────────────────────────────┐
│                      waypoint Proxy                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   역할:                                                         │
│   ═════                                                         │
│   • L7 (HTTP) 트래픽 처리                                       │
│   • VirtualService 라우팅                                       │
│   • 헤더 기반 AuthorizationPolicy                               │
│   • JWT 인증                                                    │
│   • Retry, Timeout                                              │
│   • Circuit Breaker                                             │
│   • Traffic Mirroring                                           │
│                                                                 │
│   배포 방식:                                                    │
│   ═════════                                                     │
│   • Deployment (필요할 때만)                                    │
│   • Service/Namespace 단위로 배포                               │
│   • L7 기능이 필요한 서비스에만 배포                            │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                                                         │   │
│   │  L7 불필요 서비스           L7 필요 서비스              │   │
│   │  ════════════════           ═══════════════             │   │
│   │                                                         │   │
│   │  App A ──▶ ztunnel ──▶ App B                            │   │
│   │        (mTLS만)                                         │   │
│   │                                                         │   │
│   │  App C ──▶ ztunnel ──▶ waypoint ──▶ ztunnel ──▶ App D   │   │
│   │        (mTLS)       (L7 처리)    (mTLS)                 │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Ambient Mode 활성화

### Istio 설치 시 Ambient 프로필

```bash
# Ambient 프로필로 설치
$ istioctl install --set profile=ambient

# 또는 기존 설치에 Ambient 추가
$ istioctl install --set profile=default \
    --set components.ztunnel.enabled=true \
    --set values.ztunnel.variant=distroless
```

### Namespace를 Ambient로 전환

```bash
# Namespace에 Ambient 레이블 추가
$ kubectl label namespace default istio.io/dataplane-mode=ambient

# 확인
$ kubectl get namespace default --show-labels
```

기존 Sidecar 방식과 공존 가능합니다:
- `istio-injection=enabled` → Sidecar 방식
- `istio.io/dataplane-mode=ambient` → Ambient 방식

### waypoint 배포

```bash
# Namespace에 waypoint 배포
$ istioctl waypoint apply --namespace default

# 특정 Service에만 waypoint 배포
$ istioctl waypoint apply --namespace default --name reviews-waypoint \
    --for service/reviews
```

```yaml
# 또는 YAML로 직접 배포
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: waypoint
  namespace: default
  labels:
    istio.io/waypoint-for: all  # 또는 service, workload
spec:
  gatewayClassName: istio-waypoint
  listeners:
  - name: mesh
    port: 15008
    protocol: HBONE
```

---

## 📊 트래픽 흐름

### L4 Only (mTLS만 필요한 경우)

```
┌─────────────────────────────────────────────────────────────────┐
│              L4 Only 트래픽 (waypoint 없음)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Source Pod                              Destination Pod       │
│       │                                           ▲             │
│       ▼                                           │             │
│   ┌─────────────┐                       ┌─────────────┐         │
│   │  ztunnel    │ ═══════mTLS══════════▶│  ztunnel    │         │
│   │  (Source    │                       │(Destination │         │
│   │   Node)     │                       │   Node)     │         │
│   └─────────────┘                       └─────────────┘         │
│                                                                 │
│   처리되는 것:                                                  │
│   • mTLS (SPIFFE ID 검증)                                       │
│   • L4 AuthorizationPolicy                                      │
│   • TCP 메트릭                                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### L7 필요 (waypoint 경유)

```
┌─────────────────────────────────────────────────────────────────┐
│                L7 트래픽 (waypoint 경유)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Source Pod                                  Destination Pod   │
│       │                                               ▲         │
│       ▼                                               │         │
│   ┌─────────────┐       ┌─────────────┐       ┌─────────────┐   │
│   │  ztunnel    │ ════▶ │  waypoint   │ ════▶ │  ztunnel    │   │
│   │  (Source)   │ mTLS  │  (Envoy)    │ mTLS  │(Destination)│   │
│   └─────────────┘       └─────────────┘       └─────────────┘   │
│                               │                                 │
│                               ▼                                 │
│                         L7 처리:                                │
│                         • VirtualService                        │
│                         • 헤더 기반 AuthZ                       │
│                         • JWT 인증                              │
│                         • Retry, Timeout                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📈 Ambient의 장점

### 1. 리소스 절감

```
Sidecar: Pod 100개 × Sidecar 100개 = CPU 2000m, Memory 12Gi
Ambient: Node 5개 × ztunnel 5개 = CPU 500m, Memory 1.2Gi

절감: 약 80-90%
```

### 2. 운영 간소화

```
Sidecar 업그레이드:
1. Istio 업그레이드
2. 모든 Pod 재시작 (1000개면 1000번)
3. 롤링 업데이트로 다운타임 최소화 필요

Ambient 업그레이드:
1. Istio 업그레이드
2. ztunnel DaemonSet 업데이트 (Node 수만큼)
3. Pod 재시작 불필요!
```

### 3. 점진적 도입

```yaml
# 1단계: 일부 Namespace만 Ambient
$ kubectl label namespace staging istio.io/dataplane-mode=ambient

# 2단계: 프로덕션 점진적 전환
$ kubectl label namespace production istio.io/dataplane-mode=ambient

# 기존 Sidecar 서비스와 공존 가능
```

### 4. 성능 향상

```
Sidecar 경로:
App → Sidecar(Envoy) → Network → Sidecar(Envoy) → App
       └─ 메모리 복사 ─┘           └─ 메모리 복사 ─┘

Ambient 경로 (L4 only):
App → ztunnel → Network → ztunnel → App
      └─ eBPF 직접 전달, 저오버헤드 ─┘

레이턴시 감소: 약 10-30%
```

---

## ⚠️ 고려사항

### 현재 제한사항 (2024.12 기준)

| 기능 | Sidecar | Ambient |
|------|---------|---------|
| mTLS | ✅ | ✅ |
| L7 라우팅 | ✅ | ✅ (waypoint) |
| JWT 인증 | ✅ | ✅ (waypoint) |
| EnvoyFilter | ✅ | ❌ |
| 멀티클러스터 | ✅ | ❌ (예정) |
| WASM Plugin | ✅ | ❌ (예정) |

### 언제 Sidecar를 유지할까?

- EnvoyFilter 커스터마이징 필요
- 멀티클러스터 환경
- 특수한 Envoy 설정 필요

### 언제 Ambient를 선택할까?

- 리소스 절감이 중요
- mTLS + 기본 정책이면 충분
- 신규 프로젝트 시작

---

## 📚 정리

```
┌─────────────────────────────────────────────────────────────────┐
│                   Ambient Mode 핵심 정리                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   컴포넌트                                                      │
│   ═════════                                                     │
│   • ztunnel: L4 처리 (mTLS, 기본 AuthZ), Node당 1개             │
│   • waypoint: L7 처리 (라우팅, JWT), 필요시에만 배포            │
│                                                                 │
│   장점                                                          │
│   ═════                                                         │
│   • 리소스 80-90% 절감                                          │
│   • Pod 재시작 없이 업그레이드                                  │
│   • 점진적 도입 가능                                            │
│   • 성능 향상 (저레이턴시)                                      │
│                                                                 │
│   활성화                                                        │
│   ═══════                                                       │
│   • istio.io/dataplane-mode=ambient 레이블                      │
│   • istioctl waypoint apply (L7 필요시)                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 핵심 정리

| 항목 | Sidecar | Ambient |
|------|---------|---------|
| **배포** | Pod마다 Sidecar | Node마다 ztunnel |
| **L4 처리** | Envoy Sidecar | ztunnel |
| **L7 처리** | Envoy Sidecar | waypoint (선택적) |
| **리소스** | 높음 | 80-90% 절감 |
| **업그레이드** | Pod 재시작 필요 | Pod 재시작 불필요 |

---

## 🔗 다음 편 예고

Part 2에서는 **L4/L7 분리 상세와 Sidecar 아키텍처 비교**를 다룹니다:
- ztunnel에서 처리되는 것 vs waypoint에서 처리되는 것
- HBONE 프로토콜
- Sidecar 방식과의 상세 비교

---

## 🔗 참고 자료

- [Istio Ambient Mesh](https://istio.io/latest/docs/ambient/)
- [Istio 1.24 Release - Ambient GA](https://istio.io/latest/news/releases/1.24.x/announcing-1.24/)
- [ztunnel Architecture](https://istio.io/latest/docs/ambient/architecture/ztunnel/)
- [waypoint Architecture](https://istio.io/latest/docs/ambient/architecture/waypoint/)
